<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>

<%= link_to "Add a new review", new_restaurant_path, class: "btn btn-warning"%>
